import 'package:flutter/material.dart';

class GHMModelL extends StatelessWidget {
  const GHMModelL({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hello world'),
      ),
    );
  }
}
