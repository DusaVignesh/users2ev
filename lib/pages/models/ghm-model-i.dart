import 'package:flutter/material.dart';

class GHMModelI extends StatelessWidget {
  const GHMModelI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hello world'),
      ),
    );
  }
}
