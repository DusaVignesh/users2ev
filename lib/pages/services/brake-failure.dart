import 'package:flutter/material.dart';

class BrakeFailure extends StatelessWidget {
  const BrakeFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('hell world')),
    );
  }
}
