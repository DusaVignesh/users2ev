import 'package:flutter/material.dart';

class ChainFailure extends StatelessWidget {
  const ChainFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('hell world')),
    );
  }
}
