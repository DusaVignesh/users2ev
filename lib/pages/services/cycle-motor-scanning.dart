import 'package:flutter/material.dart';

class CycleMotorScanning extends StatelessWidget {
  const CycleMotorScanning({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('hell world')),
    );
  }
}
